/** Launch UI: hide unfinished AI surfaces while keeping routes/code intact. */
export const MIXOLOGIST_LAUNCH_HIDDEN = true;

export function isMixologistLaunchHidden(): boolean {
  return MIXOLOGIST_LAUNCH_HIDDEN;
}
