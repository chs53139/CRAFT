/**
 * Mocktails use the same manifest + trusted rules as cocktails.
 * Cross-drink alcoholic proxies were removed — placeholder when no direct photo.
 */
export function resolveMocktailImageSlug(_slug: string): string | undefined {
  return undefined;
}
